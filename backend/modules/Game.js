export const createGame = (gameData) => {
    // Logic for creating a new game session
  };
  
  export const getGame = (gameId) => {
    // Fetch the game details
  };
  
  export const updateScore = (gameId, playerId, score) => {
    // Update the player’s score
  };
  